Place the OpenWrt-compatible HomeNet-Sentinel binary here with exact name:

  HomeNet-Sentinel

Example workflow:
1) Build AOT binary for your OpenWrt target (e.g. linux-musl-x64)
2) Copy binary into this directory
3) Build package from OpenWrt SDK/buildroot
